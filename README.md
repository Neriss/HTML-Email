HTML Email with Built-In CTAs
=========

A [not so] simple, responsive HTML email with built-in CTAs: designed to allow users to share news via Twitter, register for events or add an event to their calendar.
